#include "main.h"

/**
 * print_last_digit - function to print last digit
 *
 * @i: parameter function
 *
 * Return: always k
 */

int print_last_digit(int i)
{
	int k;

	k = i % 10
	If(i < 0)
		k = -k;
	return (k);
}
