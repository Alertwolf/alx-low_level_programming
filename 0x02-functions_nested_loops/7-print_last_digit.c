#include "main.h"

/**
 * print_last_digit - function that prints last digit
 *
 * @k: function parameter
 *
 * Return: k
 */

int print_last_digit(int k)
{
	int k;

	if (i < 0)
		k = -k;
	_putchar(k + '0');
	return (k);
}
