#include "main.h"

/**
 * print_last_digit - function that prints last digit
 *
 * @i: function parameter
 *
 * Return: k
 */

int print_last_digit(int i)
{
	int i;

	k = i % 10
	If(i < 0)
		k = -k;
	_putchar(k + '0');
	return (k);
}
